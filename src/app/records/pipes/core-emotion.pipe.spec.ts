import { CoreEmotionPipe } from './core-emotion.pipe';

describe('CoreEmotionPipe', () => {
  it('create an instance', () => {
    const pipe = new CoreEmotionPipe();
    expect(pipe).toBeTruthy();
  });
});
