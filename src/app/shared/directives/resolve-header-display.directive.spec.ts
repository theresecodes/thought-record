import { ResolveHeaderDisplayDirective } from './resolve-header-display.directive';

describe('ResolveHeaderDisplayDirective', () => {
  it('should create an instance', () => {
    const directive = new ResolveHeaderDisplayDirective();
    expect(directive).toBeTruthy();
  });
});
