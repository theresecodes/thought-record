import { ResolveBodyDisplayDirective } from './resolve-body-display.directive';

describe('ResolveBodyDisplayDirective', () => {
  it('should create an instance', () => {
    const directive = new ResolveBodyDisplayDirective();
    expect(directive).toBeTruthy();
  });
});
