import { ResolveAppHeaderDisplayDirective } from './resolve-app-header-display.directive';

describe('ResolveAppHeaderDisplayDirective', () => {
  it('should create an instance', () => {
    const directive = new ResolveAppHeaderDisplayDirective();
    expect(directive).toBeTruthy();
  });
});
